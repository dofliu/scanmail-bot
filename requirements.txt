# ── Core ──
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-dotenv>=1.0.0
python-multipart>=0.0.6
pydantic-settings>=2.0.0

# ── AI ──
google-genai>=1.0.0

# ── Email ──
aiosmtplib>=3.0.0

# ── Image Processing ──
Pillow>=10.0.0
img2pdf>=0.5.0
opencv-python-headless>=4.8.0
numpy>=1.24.0

# ── PDF Tools ──
pypdf>=3.0.0
reportlab>=4.0.0

# ── Document Conversion ──
python-docx>=1.0.0
markdown>=3.5.0
beautifulsoup4>=4.12.0
pymupdf>=1.23.0

# ── Video / GIF ──
moviepy>=2.0.0

# ── Security ──
cryptography>=41.0.0

# ── Auto Form Fill ──
# Layer 1 (AcroForm) 用既有 pypdf；Layer 2 用 pdfplumber 抽文字+座標
pdfplumber>=0.11.0
# Optional：Layer 3 PaddleOCR（>600MB，預設不裝；雲端部署再開）
# paddleocr>=2.7.0
# paddlepaddle>=2.5.0
