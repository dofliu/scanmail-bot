version: '3.8'

services:
  scanmail-bot:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - ../.env
    environment:
      - DATABASE_PATH=data/scanmail.db
    volumes:
      - ../data:/app/data
    restart: unless-stopped
